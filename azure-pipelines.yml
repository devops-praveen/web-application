# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool: default
  #vmImage: ubuntu-latest

steps:
-  script: echo testing-pipeline
   displayName: 'pipeline-Testing'
   
-  script: echo $(Build.BuildId)
   displayName: 'Display the Build.ID'
   
-  script: ls -al && pwd
   displayName: 'list files on working directory'
-  script: sudo apt upadte && sudo apt install -y terraform && terraform -version
   displayName: 'installing terrafrom'